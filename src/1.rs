// A simple function that takes two numbers and returns their sum
fn add(x: i32, y: i32) -> i32 {
    x + y
}

// A simple function that takes two numbers and returns their product
fn multiply(x: i32, y: i32) -> i32 {
    x * y
}
